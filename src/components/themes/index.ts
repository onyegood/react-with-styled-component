export interface ThemeProps {
  id: string;
  primaryColor: string;
  secondaryColor: string;
  successColor: string;
  dangerColor: string;
  whiteColor: string;
  blackColor: string;
  textColor: string;
  lightGray: string;
  grayColor: string;
  spinnerBg: string;
}
